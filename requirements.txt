# ============================================================
# requirements.txt — Full dev environment
# Used for: EDA, precompute.py, sanity checks
# Install: pip install -r requirements.txt
# ============================================================

# Core data
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=12.0.0        # for .parquet files

# Embeddings (pulls in torch automatically)
sentence-transformers>=2.2.0

# Excel output
openpyxl>=3.1.0

# Utilities
tqdm>=4.65.0           # progress bars for 100K candidate loop
pyyaml>=6.0            # for reading submission_metadata.yaml
