# ============================================================
# requirements-rank.txt — Ranking step only
# Used for: rank.py (the 5-min, CPU-only, no-network step)
# Install: pip install -r requirements-rank.txt
# ============================================================
# NOTE: rank.py loads pre-saved numpy arrays and parquet files.
# It never loads a model or calls any API.
# This intentionally minimal list proves compute-constraint compliance.

pandas>=2.0.0
numpy>=1.24.0
pyarrow>=12.0.0
openpyxl>=3.1.0
